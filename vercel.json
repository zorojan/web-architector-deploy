{
  "version": 2,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://<API_DOMAIN>/api/:path*"
    },
    {
      "source": "/admin/:path*",
      "destination": "https://<ADMIN_DOMAIN>/:path*"
    }
  ]
}
